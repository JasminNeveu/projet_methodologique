\documentclass[a4paper,11pt]{article}
\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{lipsum}
\usepackage{amsfonts}
\usepackage{bm}
\usepackage{amsthm}
\usepackage{amsmath}
\usepackage{float}
\usepackage[colorlinks=true,
            linkcolor=black,
            citecolor=blue,
            urlcolor=blue]{hyperref}
\usepackage{bookmark}
\usepackage{caption}
\usepackage{etoolbox}
\usepackage{booktabs}
\usepackage{array}
\usepackage{dsfont} %for \mathds
%\pretocmd{\section}{\clearpage}{}{}
\captionsetup[figure]{justification=centering}
\usepackage{subcaption}
\theoremstyle{plain}

\usepackage{changes}
\newcommand{\Javier}[1]{\textcolor{purple}{#1}}
\newcommand{\Jasmin}[1]{\textcolor{brown}{#1}}

\newcommand{\abs}[1]{\left\lvert#1\right\rvert}
\newcommand{\norm}[1]{||#1||}

\newcommand{\1}{\mathbf{1}}
\newcommand{\projnu}{\pi_{\nu}^{\perp}}
\newcommand{\indep}{\perp \!\!\! \perp}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{cor}[theorem]{Corollary}
\newtheorem{assumption}{Assumption}


\newtheorem{proposition}{Proposition}
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}

\title{Methodological project report}
\author{Jasmin Neveu}
\date{\today}

\begin{document}



\begin{titlepage}
    \centering

    {\Large ENSAI \par}
    \vspace{2cm}
    {\large ATPA Track \par}
    {\large Academic Year 2024--2025 \par}
    \vspace{2cm}
    {\Huge\bfseries Methodological Project Report \par}
    \vspace{0.4cm}
    {\LARGE\itshape Selective Inference for Hierarchical Clustering \par}
    \vspace{2cm}
    \begin{flushleft}
        \textbf{Student:} Jasmin Neveu \\
        \vspace{0.2cm}
        \textbf{Supervisor:} Javier González-Delgado
    \end{flushleft}
    \vspace{1cm}
    \begin{flushleft}
        \textbf{Analyzed article:} \\
        Lucy L. Gao, Jacob Bien, and Daniela Witten, \\
        \textit{Selective Inference for Hierarchical Clustering}, \\
        Journal of the American Statistical Association,\\ 119(545), 332–342, 2024.\\
    \end{flushleft}
    \vspace{1cm}
    {\large Submission date: \today \par}
\end{titlepage}

\tableofcontents
\newpage

\section{Introduction}
A DIRE:
\Jasmin{ concept de double dip in inference ie similaire à overfitting en prediction }
\begin{enumerate}
  \item tester si moyennes egal est tres frequent
  \item mais on peut pas simplement faire des t-test parce que data sont déjà utilisées par algo clustering
  \item donc faut trouver d'autres solutions -> selective inference
  \item c'est le but de l'article qu'on étudie qui propose une solution de selective infenrece post clustering
\end{enumerate}

In general, clustering methods do not provide theoretical guarantees regarding the partitions they generate in standard scenarios.
To ensure that the results provide guarantees on the actual differences between groups, it is very common to perform tests for equality of means between the different clusters.

However, employing a standard z-test or Wald test results in an inflation of the type I error rate \cite{Gao}. This arises from the fact that the null hypothesis is constructed adaptively, it is defined only after observing the data and is conditioned on the outcome of the clustering algorithm.

This issue is analogous to overfitting in supervised learning frameworks.
When the same dataset is utilized for both model training and evaluation, the resulting model often fails to generalize, capturing noise rather than underlying patterns. 
In the present context, the use of the same data for selection and inference is referred as \textit{double dipping}. Performing statistical inference without accounting for the variability induced by the clustering procedure results in a failure to maintain the type I error rate.

To address this issue, Gao et al. developed a valid test for the difference in means between two clusters estimated via an hierarchical agglomerative clustering algorithm.


\section{Classical versus selective testing}\label{sec:testing}

\subsection{Hypothesis testing and \textit{p-values}}
Let $(\Omega, \mathcal{F},\mathbb{P})$ be a probability space, $(E,\mathcal{E})$ a topological space and $\mathcal{T}$ the $\sigma$-algebra generated by $\mathcal{E}$. A \textit{random variable} is a measurable function $X:(\Omega,\mathcal{F})\rightarrow(E,\mathcal{T})$. The \textit{(probability) distribution} of $X$ is the mapping $P:\mathcal{T}\rightarrow[0,1]$ such that  $P(O) = (\mathbb{P}\circ X^{-1})(O)$ for all $O\in\mathcal{T}$. We say that $P$ is \textit{supported} on $E$ and denote by $\mathcal{M}_1(E)$ the set of all probability distributions supported on $\mathcal{E}$. From now on, we will set $E=\mathbb{R}$ and simply write $\mathcal{M}(\mathbb{R})=\mathcal{M}$.

\begin{definition}[Hypothesis, Definition 1.1 in~\cite{Ramdas}]\label{def_dominate_sto}
  A \textit{hypothesis} $\mathcal{H}_0$ is a set of probability distributions in $\mathcal{M}$.
  A hypothesis is \textit{simple} if it is a singleton, such as $\lbrace P \rbrace$ or $\lbrace Q \rbrace$, and \textit{composite} otherwise. The complementary set $\mathcal{M} \setminus \mathcal{H}_0$ is called the \textit{alternative hypothesis} of $\mathcal{H}_0$.
\end{definition}

\begin{definition}[Test]\label{def_test}
  A \textit{test} for $\mathcal{H}_0$ is a binary partition of the sample space, defined by a mapping:
 \begin{equation}
  \begin{array}{rcl}
    \pi_{\mathcal{H}_0} : \Omega &\to& \{0,1\} \\
    \omega &\mapsto& \pi_{\mathcal{H}_0}(\omega).
  \end{array}
 \end{equation}
 For $\omega \in \Omega$, we say that the test \textit{rejects}
 $\mathcal{H_0}$ based on $\omega$ if $\pi_{\mathcal{H}_0}(\omega)=1$, and \textit{does not reject}
  $\mathcal H_0$ based on $\omega$ if $\pi_{\mathcal{H}_0}(\omega)=0$.
\end{definition}

In what follows, we will adopt the more standard notation:
\begin{equation*}
\mathbb{P}\left(\text{Reject } \mathcal{H}_0\right) = \mathbb{P}\bigl(\lbrace \omega \in \Omega\,:\,\pi_{\mathcal{H}_0}(\omega)=1\rbrace\bigr),
\end{equation*}
making the dependence on $\pi$ implicit.

\begin{definition}[Type I error]\label{def_type_error}
  We say that a test controls the type I error at level $\alpha$ if
 \begin{equation*}
    \mathbb{P}(\emph{Reject }\mathcal{H}_0) \leq \alpha, \quad \text{for }\alpha \in (0,1).
  \end{equation*}
  We say that it controls  the type I error exactly at level $\alpha$ if
  \begin{equation*}
    \mathbb{P}(\emph{Reject }\mathcal{H}_0) = \alpha, \quad\text{for } \alpha \in (0,1).
  \end{equation*}
\end{definition}

\begin{definition}[Stochastic dominance]\label{def_sto_def}
  Let $X$ and $Y$ be real-valued random variables.
  We say that $Y$ stochastically dominates $X$, and write
  \begin{equation*}
    X \preceq_{\mathrm{st}} Y,
  \end{equation*}
  if
  \begin{equation}
    \mathbb{P}(X \leq u) \geq \mathbb{P}(Y \leq u), \quad \forall\, u \in \mathbb{R}.
  \end{equation}
\end{definition}

\begin{definition}[Super-uniform random variable]\label{def_super_unif}
  Let $X$ be a real-valued random variable. We say that $X$ is super-uniform on [0,1], and write
  $X \sim \mathcal{SU}(0,1)$, if $X$ stochastically dominates a uniform random
  variable on $[0,1]$, that is, if
  \begin{equation}
    \mathbb{P}(X \leq u) \leq u, \quad \forall\, u \in [0,1].
  \end{equation}
\end{definition}

\begin{proposition}\label{prop_repartition}
  Let $X$ and $Y$ be real-valued random variables, with cumulative distribution functions $F_X$ and $F_Y$, respectively.
  If $X \preceq_{\mathrm{st}} Y$, then $F_X(Y) \sim \mathcal{SU}(0,1)$.
\end{proposition}


\begin{remark}\label{remark_FXX_unif}
If \(X=Y\), then \(F_X(X)\) is super-uniform on \([0,1]\).  
Moreover, if \(X\) has a continuous distribution function \(F_X\), then
\begin{equation}
F_X(X) \sim \mathcal{U}(0,1).
\end{equation}
\end{remark}

\begin{definition}[$p$-value, Definition 1.1 in~\cite{Ramdas}]\label{def_pvalue}
  Let $\mathcal{H}_0$ be a hypothesis. A $p$-value for $\mathcal{H}_0$ is a super-uniform random variable under $\mathcal{H}_0$. 
\end{definition}

We often build tests using $p$-values as $\pi = \mathds{1}\{p \leq \alpha \}$.

\begin{proposition}\label{prop_rejection_rule}
  Let $\mathcal{H}_0$ be a hypothesis and $p$ a $p$-value for $\mathcal{H}_0$. Then, the test $\pi = \mathds{1}{\{p \leq \alpha\}}$ controls the type I error at level $\alpha$, for $\alpha\in(0,1)$. 
\end{proposition}

\begin{definition}[Test statistic]
  A test statistic is a measurable function $T : \Omega \to \mathbb{R}$.
\end{definition}


From now on, we will consider the case of unilateral tests.
In this setting, to define a test based on the information given by a real-valued random variable $X$, $p$-values are often built in the form: 
\begin{equation}\label{unilateral_uniform_pvalue}
  p(x) = \mathbb{P}_{\mathcal{H_0}}\bigl(T(X) \geq T(x)\bigr),
\end{equation}
where $T$ is a test statistic and $x$ a realization of $X$.

The $p$-value \eqref{unilateral_uniform_pvalue} follows a uniform distribution under $\mathcal{H}_0$. However, we have defined $p$-values as being super-uniform random variables under the null hypothesis, which corresponds to a more general family of distributions. To adapt the classical form \eqref{unilateral_uniform_pvalue} to that setting, we introduce the following proposition.

\begin{proposition}\label{prop_test_stat_pvalue}
Let \(T\) and \(T'\) be two test statistics. Let \(X\) be a random variable and \(x\) a realization of \(X\).
Define
\begin{equation}
p(x) = \mathbb{P}_{\mathcal{H}_0}\!\bigl(T'(X) \ge T(x)\bigr).
\end{equation}
If
\begin{equation}
T'(X) \preceq_{\mathrm{st}} T(X)
\quad \text{under } \mathcal{H}_0,
\end{equation}
then \(p\) is a $p$-value for \(\mathcal{H}_0\).
\end{proposition}
\begin{remark}\label{remark_equal_statistics}
If \(T' = T\) and the cumulative distribution function \(F_T\) of \(T(X)\) is continuous
under \(\mathcal{H}_0\), then
\[
p \;\overset{\mathcal{H}_0}{\sim}\; \mathcal{U}(0,1).
\]
\end{remark}


In the classical setting, the null hypothesis is independent of the data. However, in many practical applications  $\mathcal{H}_0$ is chosen \textit{after seeing the data}. In this framework, the classical testing approaches built for type I error control are unsuitable. Instead, statistical guarantees need to be provided via \textit{selective inference}. In particular, the theory of statistical testing of data-driven null hypotheses is known as \textit{selective testing}~\cite{fithian}. The next section provides some examples that motivate this framework. 

\subsection{Examples motivating selective testing}

\subsubsection{Lasso}

To determine whether an explanatory variable helps explain a response variable through a linear regression model, a common practice is to test whether its associated coefficient is significantly different from zero.
In the context of classical linear regression, this falls within the framework of non-selective inference, because all coeficients are fixed \textit{a priori}. In contrast, Lasso regression uses an \(\ell_1\)-penalty to perform variable selection  \cite{10.1111/j.2517-6161.1996.tb02080.x}.
As only the coefficients selected by the Lasso can be tested, the null hypothesis depends on the outcome of the Lasso regression, and is therefore data-driven. This corresponds to a setting of selective inference, where the classical control of type I error fails. 

To illustrate the unsuitability of non-selective inference, consider a centered Gaussian vector $X=(X_1,\ldots,X_8)$ and set the model
\[
Y = X \beta +  \varepsilon, \quad \varepsilon \sim \mathcal{N}(0,1), \quad \beta = (3,1.5,0,0,2,0,0,0)^\top.
\]
We generated $n=100$ independent realizations of $(X,Y)$ and implemented the Lasso algorithm, obtaining the regularization path presented in Figure~\ref{fig:lasso_path}(a).
Then, we tested whether a randomly chosen coefficient selected by Lasso regression and among the trully null coefficients, equals 0.
After repeating this pipeline $M=2000$ times, we obtained the empirical $p$-value distribution depicted in Figure \ref{fig:lasso_path}(b).
We clearly see that the $p$-values are not super-uniform, motivating the use of a selective testing approach instead of naive testing after selection. 

\begin{figure}[H]
  \centering
  \begin{subfigure}{0.47\textwidth}
  \includegraphics[width = \textwidth]{images/lasso_path.png} 
	\caption{}
 \end{subfigure}
 	 \begin{subfigure}{0.47\textwidth}
 	 \includegraphics[width = \textwidth]{images/lasso_pvalues.png}
  \caption{}
  \end{subfigure}
  \caption{(a) Regularization path of the coefficients obtained using the Lasso algorithm with a single realization of $(X,Y)$. Some coefficients are zero, and three converge toward the theoretical values 
(3,1.5,2). (b) Empirical cumulative distribution function (ECDF) of the $p$-values obtained from tesing wheter a randomly chosen coefficient is null after selecting the coefficient by a Lasso regression. The ECDF was computed from $M$ = 2000 simulations.}
  \label{fig:lasso_path}
\end{figure}


%\begin{figure}[H]
%  \centering
%  \includegraphics[width = 0.7\textwidth]{images/pvalues_lasso.png}
%  \caption{Cumulative distribution function of the $p$-values obtained from significance tests
%of the third coefficient estimated by a Lasso regression. The empirical distribution function
%was computed using $M$ = 2000 simulations.}
%  \label{fig:pvalue_lasso}
%\end{figure}


\subsubsection{Publication bias}


In many areas of research, scientists tend to test for signficance only when the associated effect is found to be substantial. In other words, testing is performed after a selection process that filters out small effects. In that setting, controlling type I error at a fixed level $\alpha$ yields inflated false positive rates when considering the ensemble of all published studies. 
If $Y_i \sim \mathcal{N}(\mu_i,1)$ represents the effect size in a scientific study, whose significance is tested only if  $|Y_i| > 1$, a naive level $\alpha$ test $H_{0,i}: \mu_i = 0$ is invalid. Indeed, Fithian \cite{fithian} demonstrates that the false positive rate among true nulls reaches approximately 0.16, far exceeding the nominal 0.05 level. Valid inference requires thresholding $|Y_i|$ at 2.41 rather than 1.96, the $0.95$ quantile of the standard normal, imposing a more stringent criterion.


\subsubsection{Clustering}\label{sec:clustering}

Another remarkable example of selective inference appears when evaluating the performance of clustering algorithms by testing for the equality of cluster means.
To illustrate the need of using appropriate tests in the context of selective inference, we simulated $n = 100$ samples of a five-dimensional standard Gaussian random vector. Each sample was classified into $K = 3$ groups using hierarchical agglomerative clustering and $k$-means. Then, the equality of cluster means was tested using a classical t-test, for two randomly selected clusters. If the test controlled the type I error in this setting, the resulting $p$-value would be super-uniformly distributed under the null hypothesis. However, the `clustering + post-selection testing' procedure leads to a deviation from super-uniformity, as shown in Figure~\ref{fig:deux_images}.

\begin{figure}[H]
 
 \begin{subfigure}{0.47\textwidth}
  \centering
  \includegraphics[width=\textwidth]{images/hac_pvalues.png}%
  \caption{}
\end{subfigure}  
  \hfill
  \begin{subfigure}{0.47\textwidth}
  \includegraphics[width=\textwidth]{images/kmeans_pvalues.png}
	\caption{}  
  \end{subfigure}

  \caption{Empirical cumulative distribution functions of the $p$-values obtained after testing the equality of cluster means 
    for (a) hierarchical agglomerative clustering (HAC) and (b) $k$-means algorithms.
The distribution functions were computed using $M$ = 2000 simulations from a multivariate centered normal distribution.
}
  \label{fig:deux_images}

\end{figure}


\subsection{Addressing selective testing}

Since the type I error is no longer controlled, the definition of alternative error risks is required in this setting.
Accordingly, we present three methods as described by Yoav Benjamini \cite{Benjamini}.

\subsubsection{Simultaneous Inference}

This approach controls the family-wise error rate across all hypotheses: 
\[
\mathbb{P}(\text{At least 1 false positive among all hypotheses}) \leq \alpha.
\]

This strategy proves highly conservative, ensuring that for every possible set of hypotheses, the probability of at least one false positive remains below $\alpha$. To date, no post-clustering inference approaches based on this type of control have been proposed.

\subsubsection{Sample splitting}

This approach consists in splitting the dataset into a training set $X$ and a test set $Y$.  
The training set $X$ is used to choose the null hypothesis $\mathcal{H}_0(X)$. Then, inference is performed on the test set $Y$.

Although relatively simple to implement, this method raises several issues.  
First, statistical guarantees on the tests hold only if $X$ and $Y$ are independent, which is rarely the case in practice.  
In addition, comparing cluster means on the observations in $Y$ requires assigning each test point to one of the clusters obtained from the clustering performed on $X$, a step that compromises validity, as it reuses the dataset $X$ for two separate steps. As discussed in \cite{Gao}, this strategy does not yield valid post-clustering inference in general.


\subsubsection{Conditional Inference}\label{sec:condi_inf}

This approach constitutes the most extensively studied framework for post-clustering inference.  
It controls the false positive rate conditional on hypothesis selection:  
\begin{equation}\label{condinf}
\mathbb{P}(\text{Reject } \mathcal{H}_0(X) \mid \mathcal{H}_0 \text{ selected}) \leq \alpha.
\end{equation}

In the remainder of the article, we employ this method to develop a statistical procedure for testing equality of cluster means following a clustering algorithm.

\section{Post-clustering inference}\label{sec:post_clustering_inf}

The article under study~\cite{Gao} was the first one to propose a feasible solution to post-clustering inference. Here, we will focus on the setting and approach introduced by the authors.

\subsection{Gao \textit{et al.}'s approach}\label{sec:gao}

Let $\mathbf{X} =(X_{ij})_{ij} \in \mathbb{R}^{n \times p}$ be the design matrix.
A cluster is an element of a partition of the samples.
We note $C$ a clustering algorithm and $C_1 \in C(\mathbf{X})$ a cluster obtained by the algorithm $C$ on $\mathbf{X}$. We note $\boldsymbol{\mu} = (\mu_{ij})_{ij}$ where $\mu_{ij} = \mathbb{E}[X_{ij}]$ and, for a subset $G$ of $\{1,\ldots,n\}$, we write
\begin{equation*}
\bar{\mu}_G = \dfrac{1}{|G|} \displaystyle\sum_{i \in G} \mu_i \in \mathbb{R}^p\quad\text{and}\quad\bar{X}_G = \dfrac{1}{|G|} \displaystyle\sum_{i \in G} X_i \in \mathbb{R}^p.
\end{equation*}
The goal is to test for the equality of cluster means, that is, testing the following null hypothesis: 
\begin{equation}
  \mathcal{H}_0^{\{C_1,C_2\}}: \bar{\mu}_{C_1} = \bar{\mu}_{C_2},
\end{equation}
for a pair of clusters $C_1,C_2$. For concision and clarity, we introduce the $n$-dimensional vector
\begin{equation}\label{nu}
\nu(C_1,C_2)=\nu = (\nu_i)_{i=1}^n, \qquad
\nu_i =
\begin{cases}
\dfrac{1}{|C_1|}, & \text{if } i \in C_1, \\[0.4em]
-\dfrac{1}{|C_2|}, & \text{if } i \in C_2, \\[0.2em]
0, & \text{otherwise},
\end{cases}
\end{equation}
such that the population (resp. empirical) difference of cluster means can be compactely rewritten as:
\begin{equation*}
  \left\{
    \begin{aligned}
      \bar{\mu}_{C_1} - \bar{\mu}_{C_2} &= \nu^T\boldsymbol{\mu} \\
    \bar{X}_{C_1} - \bar{X}_{C_2} &= \nu^T\mathbf{X}.
    \end{aligned}
    \right.
\end{equation*}

Thus, we can rewrite the null hypothesis as follows:
\begin{equation}\label{h0c1c2}
 \mathcal{H}_0^{\{C_1,C_2\}}: \bar{\mu}_{C_1} = \bar{\mu}_{C_2} \iff \nu^T\boldsymbol{\mu} = 0.
\end{equation}

From now on, we adopt the conditional inference framework, described in Section \ref{sec:condi_inf}. In this setting, controlling the risk~\eqref{condinf} is equivalent to controlling the so-called \textit{selective type I error for clustering}, defined below.
\begin{definition}[Selective type I error for clustering]
  We say that a test controls the selective type I error for clustering at leval $\alpha$ if
  \begin{equation}
    \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\bigl(\text{Reject} ~ \mathcal{H}_0^{\{C_1,C_2\}} ~ | ~ C_1,C_2 \in C(\mathbf{X})\bigr) \leq \alpha,\quad \alpha \in (0,1).
  \end{equation}
  We say that it controls exactly the selective type I error for clustering at level $\alpha$ if
  \begin{equation}
  \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\bigl(\text{Reject} ~ \mathcal{H}_0^{\{C_1,C_2\}} ~ | ~ C_1,C_2 \in C(\mathbf{X})\bigr) = \alpha, \quad \alpha \in (0,1).
\end{equation}
\end{definition}
To define a $p$-value that controls the selective type I error, model assumptions need to be imposed on $\mathbf{X}$. The authors in \cite{Gao} adopt the following matrix normal model:
\begin{equation}\label{matrixnormal}
\mathbf{X}\sim\mathcal{MN}_{n\times p}(\boldsymbol{\mu},\mathbb{I}_n,\sigma^2\mathbb{I}_p),
\end{equation}
which means that the lines of $\mathbf{X}$ are independent $p$-dimensional random vectors distributed as $\mathcal{N}_p(\mu_i,\sigma^2\mathbb{I}_p)$, for every line $i\in\lbrace 1,\ldots,n\rbrace$. Note that this model imposes an independence assumption between the features (columns) of $\mathbf{X}$.

To run a proper test, we need to control the selective type I error at level $\alpha$.
Ideally, we would like to define a p-value as follows: 
\[
  p_{ideal}(\mathbf{x}) = \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\bigl(T(\mathbf{X}) \geq T(\mathbf{x}) ~ | ~ C_1,C_2 \in C(\mathbf{X})\bigr),
\]
with $T$ being a test statistic. In \cite{Gao}, Gao \textit{et al.} defined the test statistic as $T(\mathbf{X}) = \norm{\nu^\top\mathbf{X}}_2$, because its distribution under the null hypothesis can be straightforwardly derived. Indeed, noting that:
\begin{equation*}
\nu^\top \mathbf{X}
\sim \mathcal{MN}_{1 \times p}\bigl(\nu^\top \mu,\ \nu \nu^\top,\ \sigma^2 I_p\bigr) = \mathcal{N}_p\bigl(\nu^\top \mu,\ \|\nu\|_2^2 \sigma^2 I_p\bigr),
\end{equation*}
we have, under $\mathcal{H}_0^{\{C_1,C_2\}}$, that
\begin{equation}
  \norm{\nu^\top \mathbf{X}}_2                                \sim \norm{\nu}_2 \sigma \chi_p.
\end{equation}
However, $p_{ideal}$ cannot be evaluated in practice as it depends on unknown parameters ~\cite{Gao}. To address this issue, the authors in~\cite{Gao} propose to add technical events to the conditioning set, considering the following quantity: 
\begin{equation}\label{pvalcondi}
  p(\mathbf{x}) = \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\Bigl(\norm{\nu^T\mathbf{X}}_2 \geq \norm{\nu^T\mathbf{x}}_2 ~ | ~ C_1,C_2 \in \mathcal{C}(\mathbf{X}), \xi(\mathbf{X})\Bigr),
\end{equation}
as a $p$-value for~\eqref{h0c1c2}, with $\xi(\mathbf{X})$ being the additional events. To justify the choice of $\xi(\mathbf{X})$, we follow the steps in~\cite{Gao}. First, we decompose the data matrix as follows:
\begin{equation}
  \mathbf{X} = \projnu\mathbf{X} + (\mathbb{I}_n - \projnu)\mathbf{X},
\end{equation}
where $\projnu$ is the orthogonal projection matrix onto the subspace orthogonal to $\nu$, defined as:
\begin{equation}
  \projnu = \mathbb{I}_n - \frac{\nu\nu^\top}{\norm{\nu}_2^2} \in \mathbb{R}^{n\times n}.
\end{equation}
Then, we can write
\begin{equation}
\begin{aligned}
  \mathbf{X} &= \projnu \mathbf{X} + \frac{\nu \nu^T}{\norm{\nu}_2^2}\mathbf{X} \\
   &= \projnu \mathbf{X} + \frac{\nu}{\norm{\nu}_2^2}\frac{\nu^T \mathbf{X}}{\norm{\nu^T \mathbf{X}}_2}\norm{\nu^T \mathbf{X}}_2 \\
   &= \projnu \mathbf{X} + \frac{\nu}{\norm{\nu}_2^2} \operatorname{dir}(\nu^T \mathbf{X})\norm{\nu^T \mathbf{X}}_2,
\end{aligned}
\end{equation}
where dir denotes the direction of a vector, that is,
\begin{equation}
  \text{dir}(z) = \frac{z}{\norm{z}_2}, z \in \mathbb{R}^p.
\end{equation}
Since $\text{dir}(\nu^T \mathbf{X})$ and $\projnu \mathbf{X}$ are unknown, we condition on their values for a realization $\mathbf{x}$ of $\mathbf{X}$, defining the additional events as:
\begin{equation*}
  \xi(\mathbf{X}) = \bigl\{ \projnu \mathbf{X} = \projnu \mathbf{x}, ~ \text{dir}(\nu^T \mathbf{X}) = \text{dir}(\nu^T \mathbf{x})\bigr\}.
\end{equation*}
Plugging the previous equation in \eqref{pvalcondi} we obtain the following expression for the $p$-value:
\begin{equation}\label{pval_final}
  \begin{aligned}
p(x) &= \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}} \Bigl(
    \norm{\nu^\top \mathbf{X}}_2 \geq \norm{\nu^\top x}_2 
    \,\Big|\,
    C_1,C_2 \in \mathcal{C}(\mathbf{X}), \\ 
&\qquad\qquad
    \projnu \mathbf{X} = \projnu \mathbf{x},\,
    \text{dir}(\nu^\top \mathbf{X}) = \text{dir}(\nu^\top \mathbf{x})
\Bigr).
\end{aligned}
\end{equation}
Now that we have defined the model, the hypothesis to be tested, and the corresponding $p$-value, we will show that this $p$-value can be computed under an analitically tractable form and that it properly controls the selective type I error. For this, we rely on the following technical lemma, whose proof is presented in Appendix~\ref{app:proofs}.
\begin{lemma}\label{lemma_indep_add_events}
Under the null hypothesis, the following statements hold:
\[
  \begin{aligned}
  &\text{(i)} & \|\nu^\top \mathbf{X}\|_2 &\indep \projnu \mathbf{X},\\
  &\text{(ii)} & \|\nu^\top \mathbf{X}\|_2 &\indep \operatorname{dir}(\nu^\top \mathbf{X}).
\end{aligned}
\]
\end{lemma}

Following from the previous result, we can rewrite the  $p$-value as:
\begin{equation}
  \begin{aligned}
    p(\mathbf{x}) = \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\Bigl(\norm{\nu^\top & \mathbf{X}}_2  \geq \norm{\nu^\top \mathbf{x}}_2 ~|~ \\
  & C_1, C_2 \in \mathcal{C}(\projnu \mathbf{X} + \dfrac{\nu}{\norm{\nu}^2_2} \operatorname{dir}(\nu^\top \mathbf{x}) \norm{\nu^\top \mathbf{x}}_2  )  \Bigr).
\end{aligned}
\end{equation}
Letting $\phi = \norm{\nu^\top \mathbf{X}}_2$ and $\mathbf{x}'(\phi) = \projnu \mathbf{x} + \dfrac{\nu}{\norm{\nu}_2^2} \operatorname{dir}(\nu^\top \mathbf{x})\phi $, we can write
\begin{equation}
  \begin{aligned}
    p(\mathbf{x} &= \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\Bigl(\phi \geq \norm{\nu^\top \mathbf{x}}_2 ~|~ C_1,C_2 \in \mathcal{C}\bigl(\mathbf{x}'(\phi)\bigr)\Bigr) \\
         &= \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\Bigl(\phi \geq \norm{\nu^\top \mathbf{x}}_2 ~|~ \phi \in \mathcal{S}\bigr(\mathbf{x}; \{C_1,C_2\}\bigl)\Bigr),
\end{aligned}
\end{equation}
where
\begin{equation}\label{set_S}
\mathcal{S}\bigl(\mathbf{x},\{C_1,C_2\}\bigr) = \Bigl\{\phi \geq 0: C_1,C_2 \in \mathcal{C}\bigl(\mathbf{x}'(\phi)\bigr)\Bigr\}.
\end{equation}
As, under the null, we have that $\phi \sim \norm{\nu}_2\sigma\chi_p$, we can finally write:

\begin{equation}\label{pvalue_tractable}
  p(\mathbf{x}) = 1 - \mathbb{F}_p\Bigl(\nu^\top \mathbf{x};\norm{\nu}_2\sigma;\mathcal{S}\bigl(\mathbf{x};\{C_1,C_2\}\bigr)\Bigr),
\end{equation}
where $\mathbb{F}_p(t;c;\mathcal{S})$ denotes the cumulative distribution function of a $c\cdot\chi_p$ random variable truncated to the set $\mathcal{S}$. This proves that the $p$-value~\eqref{pval_final} can be written in the analitically tractable form~\eqref{pvalue_tractable}. This corresponds to the first statement in~\cite[Theorem 1]{Gao}, that we present below.

\begin{theorem}[Theorem 1 in \cite{Gao}]\label{thm:1}
  For any non-overlapping groups of observations $C_1,C_2 \subseteq \{1,...,n\}$ and any realization $\mathbf{x}$ of $\mathbf{X} \sim \mathcal{MN}_{n \times p}(\mathbf{\mu},\mathbb{I}_n,\sigma^2\mathbb{I}_p)$

  \begin{equation}
  p(\mathbf{x}) = 1 - \mathbb{F}\bigl(\nu^\top \mathbf{x};\,\norm{\nu}_2 \sigma;\,\mathcal{S}\bigl(\mathbf{x};\{C_1,C_2\}\bigr)\bigr)
  \end{equation}


  Furthermore, if $\mathcal{H}_0^{\{C_1,C_2\}}$ is true, then
  \begin{equation}\label{riskcontrol}
    \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\Bigl(p(\mathbf{x}) \leq \alpha ~|~ C_1,C_2 \in \mathcal{C}(\mathbf{X})\Bigr) = \alpha,\quad \forall\, 0 \leq \alpha \leq 1.
  \end{equation}

\end{theorem}
The control of the selective type I error is proved in Appendix~\ref{app:proofs}. After a few manipulations we can rewrite $\mathbf{x}'(\phi)$ as follows:
\begin{equation}
  \begin{aligned}
    \mathbf{x}'(\phi) &= \projnu \mathbf{x} + \frac{\nu}{\norm{\nu}_2} \operatorname{dir}(\nu^\top \mathbf{x}) \phi \\
                      &= \mathbf{x} - \frac{\nu \nu^\top}{\norm{\nu}_2^2}\mathbf{x} + \frac{\nu}{\norm{\nu}_2}\operatorname{dir}(\nu^\top \mathbf{x})\phi \\
                      &= \mathbf{x} + \frac{\nu}{\norm{\nu}_2^2}(\phi \operatorname{dir}(\nu^\top \mathbf{x}) - \nu^\top \mathbf{x}) \\
                      &= \mathbf{x} + \frac{\nu}{\norm{\nu}_2^2}(\phi \operatorname{dir}(\nu^\top \mathbf{x}) - \norm{\nu^\top \mathbf{x}}_2 \operatorname{dir}(\nu^\top \mathbf{x}) ) \\
                      &= \mathbf{x} + \frac{\nu}{\norm{\nu}_2^2}\operatorname{dir}(\nu^\top \mathbf{x})(\phi - \norm{\nu^\top \mathbf{x}}_2)\added{,}
  \end{aligned}
\end{equation}
so that $\mathbf{x}'(\phi)$ can be interpreted as a perturbed version of $\mathbf{x}$. In $\mathbf{x}'(\phi)$, the compared clusters are either pushed appart or pulled together depending on the value of $\phi$. When $\phi\leq\norm{\nu^T\mathbf{x}}$, the smaller $\phi$ is, the more the clusters are pulled together. If $\phi>\norm{\nu^T\mathbf{x}}$, the larger $\phi$ is, the more separate they are. More details are provided in Section 2.2 in \cite{Gao}. Here, we illustrate these perturbations in Figure \ref{fig:deux_images_perturbated_data}.

\begin{figure}[H]
 \begin{subfigure}{0.30\textwidth}
  \centering
  \includegraphics[width=\textwidth]{images/original_data.png}%
  \caption{}
\end{subfigure}  
  \hfill
  \begin{subfigure}{0.30\textwidth}
  \includegraphics[width=\textwidth]{images/perturbated_0_data.png}
	\caption{}  
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.30\textwidth}
    
    \includegraphics[width=\textwidth]{images/perturbated_8_data.png}
    \caption{}
  \end{subfigure}
  \caption{(a) Original dataset $\mathbf{x}'(\phi) = \mathbf{x}$ with $\phi  =\norm{\nu^T\mathbf{x}}\approx 4.5$, (b) perturbated dataset $\mathbf{x}' (\phi)$ with $\phi = 0$ and (c) a perturbated dataset $\mathbf{x}'(\phi)$ with $\phi = 8$. In all cases, $C_1$ and $C_2$ in $\nu(C_1,C_2)$ correspond to the red (1) and green (2) clusters.}
  \label{fig:deux_images_perturbated_data}
\end{figure}

Thus, following from~\eqref{set_S}, $\mathcal{S}$ corresponds to the set of perturbations that preserve the clusters to be compared. \Javier{Note that we don't ask all the clusters in $\mathcal{C}(\mathbf{x})$ to be preserved, but only $C_1$ and $C_2$.} The efficient determination \Javier{To avoid saying \textit{computation} twice.} of such set is required for the computation of~\eqref{pval_final}. This can be carried out thanks to the strategies proposed by Gao \textit{et al.} for several HAC algorithms (see~\cite[Section 3]{Gao} for details).

\subsection{Numerical experiments}

\subsubsection{Uniformly distributed $p$-values under the null hypothesis}\label{sec:unif_selec}

Section \ref{sec:gao} detailed the approach of Gao \textit{et al.} for testing the equality of cluster means after implementing an HAC algorithm. \Javier{As `HAC' is pronounced `aitch-A-C', starting with a vowel sound, we write \textit{an} before.} In Theorem~\ref{thm:1}, we showed that the $p$-value defined in \eqref{pval_final} controls the selective type I error for clustering.

To empirically illustrate this result, we simulated the null distribution of $p$-values, following the same strategy as in Section \ref{sec:clustering} but this time using~\eqref{pval_final} as a $p$-value for~\eqref{h0c1c2}. For the simulations based on the HAC algorithm, we used the \texttt{clusterpval} library developed by Gao \textit{et al.}, which implements the $p$-value \eqref{pvalue_tractable}. For the simulations based on the $k$-means algorithm, we used the \texttt{KmeansInference} library \cite{JMLR:v24:22-0371}, which relies on a similar $p$-value adapted to the $k$-means procedure. The corresponding results are presented in Figure \ref{fig:deux_images_selective}, which shows that the resulting $p$-values are indeed super-uniform under the null hypothesis.

\begin{figure}[H]
 \begin{subfigure}{0.47\textwidth}
  \centering
  \includegraphics[width=\textwidth]{images/hac_pvalues_selective.png}%
  \caption{}
\end{subfigure}  
  \hfill
  \begin{subfigure}{0.47\textwidth}
  \includegraphics[width=\textwidth]{images/kmeans_pvalues_selective.png}
	\caption{}  
  \end{subfigure}
  \caption{Empirical cumulative distribution functions of the $p$-values obtained after testing the equality of cluster means 
    for (a) hierarchical agglomerative clustering (HAC) and (b) $k$-means algorithms.
The empirical cumulative distribution functions (ECDF) were computed using $M$ = 2000 samples drawn from a multivariate centered normal distribution, as detailed in Section~\ref{sec:clustering}.
}
  \label{fig:deux_images_selective}
\end{figure}


\subsubsection{Robustness to feature dependence}\label{sec:rob_data}

The control of the selective type I error for clustering relies on a strong assumption: the features must be independent \eqref{matrixnormal}. To assess whether the method is robust to this assumption, we repeated the strategy in the previous section and simulated the null distribution of $p$-values  for $M=2000$ realizations of:
\begin{equation}\label{big_sigma_model}
  \mathbf{X} \sim \mathcal{MN}_{n \times p}\bigl(\boldsymbol{\mu},\mathbb{I}_n,\boldsymbol{\Sigma} \bigr),
\end{equation}
with \begin{equation}\label{big_sigma}
  \boldsymbol{\Sigma} = \rho^{|i-j|} ~\text{for}~ 1 \leq i,j \leq p ~\text{and}~ \rho \in (0,1].
\end{equation}
Figure \ref{fig:images_grad_rho} shows that the $p$-values are no longer super-uniform under the null hypothesis, and therefore the selective type I error is no longer controlled when the independence assumption is not satisfied. Besides, we observe that the deviation from uniformity increases with the dependency parameter $\rho$. Indeed, the stronger the dependence between the features, the less the selective type I error is controlled.
The deviation from super-uniformity also increases with the dimension, as illustrated in Figure \ref{fig:images_grad_rho} (c-d).

\begin{figure}[H]
  \centering

  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/hac_pvalues_grad_rho.png}
    \caption{}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/kmeans_pvalues_grad_rho.png}
    \caption{}
  \end{subfigure}

  \vspace{0.5cm}

  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/hac_pvalues_grad_p.png}
    \caption{}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/kmeans_pvalues_grad_p.png}
    \caption{}
  \end{subfigure}

  \caption{Empirical cumulative distribution functions (ECDF) of the $p$-values obtained after testing the equality of cluster means 
    for hierarchical agglomerative clustering (HAC). 
    The distribution functions were computed using $M$ = 2000 samples drawn from~\eqref{big_sigma_model} with correlation matrix ~\eqref{big_sigma} for (a-b) different values of $\rho$ , with fixed $p=5$, and (c-d) different values of $p$, for fixed $\rho=0.5$ .\Javier{Complete with the fixed value of $p$ that you set for (a-b).}}
  \label{fig:images_grad_rho}
\end{figure}


\subsection{Data applications}

To evaluate the empirical performance of the method of Gao \textit{et al.}, we analyzed the $p$-value behavior using data from the \textit{1000 Genomes Project} \added{(1KGP)~\cite{1kgp}}. \Javier{I wouldn't speak of \textit{empirical performance}, because we don't need \textit{real} data to evaluate that. I would directly say that we want to assess the suitability of the method in real cases of application, where data does not necessarily fit model~\eqref{matrixnormal}.} \Javier{Also, you should add here a brief summary of what you are doing in this section: first, you introduce genetic data (is not something that everyone knows), then you describe the clustering output when applying HAC to the 1KGP cohort, and then we analyze the performance of Gao \textit{et al.}'s $p$-value when used to provide statistical guarantees on that clustering.}

\subsubsection{Genotype data}

The DNA sequence contains all the genetic information associated with an individual. It is composed of two complementary strands made of nucleotides, denoted by A, C, G, and T. Although these sequences are very long, the vast majority of the genome is shared among all individuals. Instead of relatively comparing DNA sequences, genetic databases are constructed by comparing every individual's genome to the so-called \textit{reference genome}. This yields a matrix of genetic variation, often referred to as \textit{SNP matrix}, that serves as quantitative descriptor of the genetic variability of a human population. From this SNP matrix, a principal component analysis (PCA) can be performed in order to obtain the dataset \texttt{1KGP\_100PC}. Besides containing the genetic information of every individual, the 1KGP dataset also provides their ancestral population, that we describe in Appendix~\ref{app:data}.

\subsubsection{Clustering}

To try to capture the population structure of the 1KGP indivuduals, we perfomed HAC clustering and set the algorithm to choose $K$ clusters, with $K$ being the number of ancestral populations. Indeed, we consider the set of populations as an underlying true partition to which the clustering output will be compared. Figure \ref{fig:pca} shows the 1KGP individuals represented on the first two principal components (PCs).

\begin{figure}[H]
  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width = \textwidth]{images/PCA.png}%
    \caption{}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.47\textwidth}
    \includegraphics[width = \textwidth]{images/PCAcluster.png}%
    \caption{}
  \end{subfigure}
  \caption{Projection of the individuals onto the first two PCs, colored by population in (a) and by clusters obtained from HAC with Ward linkage on the first 100 PCs in (b).}
  \label{fig:pca}
\end{figure}
The clusters obtained with the HAC algorithm in Figure \ref{fig:pca}(b) appear to be largely consistent with the population labels of the individuals.  
This can also be assessed by computing the confusion matrix comparing the clustering output and the population classification, as shown in Figure \ref{fig:heatmap_clust_pop}.  
Overall, the heatmap exhibits an approximately diagonal structure, indicating that each cluster mostly corresponds to a single population.  
However, some populations are spread across several clusters (PJL, ASW, GIH), and some clusters contain individuals from multiple populations (1, 3, 16).
\begin{figure}[H]
  \centering
  \includegraphics[width= \textwidth]{images/heatmap_clust_pop.png}
  \caption{Confusion matrix comparing the clustering output and the population labels for the 1KGP individuals. Clusters were obtained using the HAC algorithm with Euclidean distance and the Ward linkage.}
  \label{fig:heatmap_clust_pop}
\end{figure}

\subsubsection{Suitability of Gao \textit{et al.}'s approach}

\Javier{Up to here, we have just introduced the data and performed a clustering algorithm. To introduce this section, say that now we want to apply a post-clustering inference procedure to the previously performed clustering, and explore whether Gao \textit{et al.}'s $p$-value yields results that are consistent with the confusion matrix in Figure~\ref{fig:heatmap_clust_pop}, that is, if clusters corresponding to different populations are detected as different, and clusters corresponding to a single population are not. The paragraph below starts to harsh. Try to give some context and explain that we are evaluating the control of selective type I error and the performance in terms of power.}

\Javier{Importantly! The variance parameter $\sigma$ is unknown here. We must specify how are we dealing with it. For that, we can mention Theorem 4 in~\cite{Gao} (go and see it). It says that we can plug in an over-estimator $\hat{\sigma}$ of $\sigma$ and the selective type I error remains controlled. You have to mention this result here and say that we are plugging an over-estimator provided by Gao \textit{et al.} (their Equation (C.1). I don't think we should write a theoretical part in the previous section because I don't have enough time and the proof is long (not difficult, but long). So I would just write a couple of sentences here about it, mentioning the results of Gao's paper. It is important that you know this for the defense too.}

In Section \ref{sec:rob_data}, we showed that the $p$-value is sensitive to dimensionality.
In practice, it is convenient to retain the maximum number of principal components to minimize information loss. However, this comes at a cost to the statistical guarantees of Gao \textit{et al.}’s method. To be statistically valid, it must control the selective type I error. However, a useful testing procedure should also be \textit{powerful}. Panels (a--b) in Figure \ref{fig:curse_dim_real_data} show that, for two clusters composed of individuals from the same super-population, the $p$-value falls below 0.05 (the conventional significance level) when $p$ > 12. This implies that the cluster means are considered significantly different, despite originating from the same super-population.
Furthermore, panels (c--d) show that, for two clusters containing individuals from different but closely super-populations in the first two PCs, \replaced{the $p$-value}{$p$} \Jasmin{C'est bien la dimension $p$ qui doit être plus grande que 6} must be greater than 6 for the $p$-value to drop below 0.05. Consequently, the $p$-value is valid and powerful only for a small range of $p$, which substantially limits its practical applicability.

\begin{figure}[H]
  \centering

  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/pval_12_26.png}
    \caption{}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/pval_13_8.png}
    \caption{}
  \end{subfigure}

  \vspace{0.5cm}

  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/pval_5_7.png}
    \caption{}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/pval_9_16.png}
    \caption{}
  \end{subfigure}

  \caption{$p$-values~\eqref{pval_final} obtained from testing the equality of cluster means for clusters (a) $\lbrace 12,26\rbrace$, (b) $\lbrace 8,13\rbrace$, (c) $\lbrace 5,7\rbrace$ and (d) $\lbrace 9,13\rbrace$, as a function of the number of PCs used for clustering, that is, the dimension $p$ of the feature space.}
  \label{fig:curse_dim_real_data}
\end{figure}

\subsubsection{Assessing intercontinental vs intracontinental differences}

\Javier{In the caption of Table~\ref{table:indiv}, you should write the complete name of super-populations to understand what abbreviations mean.}

Each population belongs to a super-population, corresponding to a larger grouping similar to a continent (see Table~\ref{table:indiv} in Appendix~\ref{app:data}). For example, Mexican Ancestry in Los Angeles, California (MXL) and Puerto Rican in Puerto Rico (PUR) belong to the Africa-American \Jasmin{ce n'est pas admixed-American ? cf \href{https://link.springer.com/article/10.1186/s13059-020-02229-3/tables/1?utm_source=chatgpt.com}{site}} (AMR) super-population, whereas Utah residents with Northern and Western European ancestry (CEU) belongs to the European (EUR) super-population.  
Intuitively, the $p$-values associated with tests between clusters containing individuals from different super-populations are expected to be close to 0, while the $p$-values associated with tests between clusters containing individuals from the same super-population are expected to be larger. \Javier{We would like the test to also reject in these cases, but with larger $p$-values.} To compute the $p$-values, we kept only the first 12 principal components following from the analyses presented in the previous section. The resulting $p$-values are shown in Figure \ref{fig:heatmap_pval}. \Javier{Add a few words commenting the Figure. What do we observe? Does the test perform well regarding this analysis?}

% TODO: traduire 
We observe that within each super-population, the $p$-values indicate that the clusters have equal means, as expected.
Although a few $p$-values suggest that clusters containing individuals from different super-populations also have statistically equal means, the test appears to successfully recover the super-population structure of the data.

\begin{figure}[H]
  \centering
  \includegraphics[width = \textwidth]{images/heatmap_pval.png}%
  \caption{ Heatmap of the $p$-values obtained using the \texttt{test\_hier\_clusters\_exact} function applied to the first 12 PCs, with clusters obtained from HAC using Euclidean distance and the Ward.D linkage method on the first 100 principal components.}
  \label{fig:heatmap_pval}
\end{figure}


\section{Discussion}


\Jasmin{Discussion: 1. cadre selective inference 2. procedure gao et al dans le cadre du clustering 3. puis methode dans un jeu de donnée reel => limitation puis paragraphe suivant on parle des limitations en questions.}
\Jasmin{ la methode est bien car 1er + parle d'estimation de parametres inconnu}

Research on post clustering selection is relatively recent, and the method of Gao \textit{et al.} represents the state of the art in for the conditional inference approach. While, in theory, this method defines a $p$-value that controls the selective type I error, it relies on strong assumptions that limit its practical validity.
For the $p$-value to be valid, the features must be independent and the data dimension should not be too large. When applied to real data, we have shown that it is not possible to simultaneously control the selective type I error and the power of the test.
To address these limitations, other approaches have been proposed \Jasmin{... cf ?}

\appendix

\section{Proofs}\label{app:proofs}


\subsection{Proofs of Section~\ref{sec:testing}}
\begin{proof}[Proof of Proposition~\ref{prop_repartition}]

Let \(G_X\) denote the generalized inverse (quantile function) of \(F_X\),
defined for \(u\in[0,1]\) as
\begin{equation}
G_X(u) = \inf\{x\in\mathbb{R} : F_X(x) \ge u\}.
\end{equation}

By definition of the generalized inverse,
\begin{equation}
\{F_X(Y) \le u\} = \{Y < G_X(u)\}.
\end{equation}
Therefore,
\begin{equation}
\mathbb{P}\bigl(F_X(Y)\le u\bigr)
= \mathbb{P}\bigl(Y < G_X(u)\bigr)
= F_Y\bigl(G_X(u)^-\bigr),
\end{equation}
where \(G_X(u)^-\) denotes the left limit at \(G_X(u)\).

Since \(X \preceq_{\mathrm{st}} Y\), we have \(F_Y \le F_X\) pointwise, and thus
\begin{equation}
F_Y\bigl(G_X(u)^-\bigr)
\le F_X\bigl(G_X(u)^-\bigr).
\end{equation}
By the defining property of the generalized inverse,
\begin{equation}
F_X\bigl(G_X(u)^-\bigr) \le u.
\end{equation}

Combining these inequalities yields
\begin{equation}
\mathbb{P}\bigl(F_X(Y)\le u\bigr) \leq u,
\quad \forall\, u\in[0,1],
\end{equation}
which proves that \(F_X(Y)\) is super-uniform.


\end{proof}


\begin{proof}[Proof of Remark~\ref{remark_FXX_unif}]

  By the proposition \ref{prop_repartition}, we have that $F_X(X) \sim \mathcal{SU}(0,1)$. If \(F_X\) is continuous, then \(F_X(G_X(u)) = u\) for all \(u\in[0,1]\), and hence
  \begin{equation}
\mathbb{P}\bigl(F_X(X)\le u\bigr)
= F_X\bigl(G_X(u)\bigr)
= u,
\quad \forall\,u\in[0,1],
\end{equation}
which concludes the proof.

\end{proof}


\begin{proof}[Proof of Proposition~\ref{prop_rejection_rule}]

By definition of the rejection rule,
\begin{equation}
  \mathbb{P}_{\mathcal{H}_0}(\text{Reject} ~ \mathcal{H}_0)
= \mathbb{P}_{\mathcal{H}_0}(p \le \alpha).
\end{equation}
Since \(p\) is a $p$-value for \(\mathcal{H}_0\), it is super-uniform under
\(\mathcal{H}_0\), hence
\begin{equation}
\mathbb{P}_{\mathcal{H}_0}(p \le \alpha) \le \alpha.
\end{equation}
This establishes control of the type~I error at level \(\alpha\).
\end{proof}





\begin{proof}[Proof of Proposition~\ref{prop_test_stat_pvalue}]

Let \(F_{T'(X)}\) denote the distribution
function of \(T'(X)\) under $\mathcal{H}_0$.
By Proposition ~\ref{prop_repartition}, the stochastic dominance
\(T'(X) \preceq_{\mathrm{st}} T(X)\) implies that
\[
F_{T'(X)}\bigl(T(X)\bigr) \sim \mathcal{SU}(0,1).
\]

By definition,

\begin{equation}
p(x)
= \mathbb{P}_{\mathcal{H}_0}\!\bigl(T'(X) \ge T(x)\bigr)
= 1 - F_{T'(X)}\bigl(T(x)\bigr).
\end{equation}

Let \(u \in [0,1]\). Then
\begin{align}
\mathbb{P}_{\mathcal{H}_0}\bigl(p(X) \le u\bigr)
&= \mathbb{P}_{\mathcal{H}_0}\!\left(1 - F_{T'(X)}(T(X)) \le u\right) \\
&= \mathbb{P}_{\mathcal{H}_0}\!\left(F_{T'(X)}(T(X)) \ge 1-u\right) \\
&= 1 - \mathbb{P}_{\mathcal{H}_0}\!\left(F_{T'(X)}(T(X)) \le 1-u\right).
\end{align}

Since \(F_{T'(X)}(T(X))\) is super-uniform,
\begin{equation}
\mathbb{P}_{\mathcal{H}_0}\!\left(F_{T'(X)}(T(X)) \le 1-u\right) \le 1-u,
\end{equation}
and therefore
\begin{equation}
\mathbb{P}_{\mathcal{H}_0}\bigl(p(X) \le u\bigr) \le u.
\end{equation}

Thus, \(p\) is super-uniform under \(\mathcal{H}_0\), and hence a $p$-value.
\end{proof}


\begin{proof}[Proof of Remark~\ref{remark_equal_statistics}]

When \(T'=T\)
\begin{equation}
p(X) = \mathbb{P}_{\mathcal{H}_0}\bigl(T(X') \ge T(X)\bigr)
      = 1 - F_T\bigl(T(X)\bigr),
\end{equation}

By Remark ~\ref{remark_FXX_unif}, if \(F_T\) is continuous,
\begin{equation*}
F_T\bigl(T(X)\bigr) \sim \mathcal{U}(0,1).
\end{equation*}
Consequently, for any \(u\in[0,1]\),
\begin{align}
\mathbb{P}_{\mathcal{H}_0}\bigl(p(X) \le u\bigr)
&= \mathbb{P}_{\mathcal{H}_0}\bigl(1 - F_T(T(X)) \le u\bigr) \\
&= \mathbb{P}_{\mathcal{H}_0}\bigl(F_T(T(X)) \ge 1-u\bigr) \\
&= 1 - \mathbb{P}_{\mathcal{H}_0}\bigl(F_T(T(X)) \le 1-u\bigr) \\
&= 1 - (1-u) \\
&= u.
\end{align}
Thus \(p\) is uniformly distributed on \([0,1]\) under \(\mathcal{H}_0\).
\end{proof}


\subsection{Proofs of Section~\ref{sec:post_clustering_inf}}

\begin{proof}[Proof of Lemma \ref{lemma_indep_add_events}]
  \textit{i)}
  \begin{equation}
    \begin{aligned}
    \mathbb{E}(\nu^\top\mathbf{X} (\projnu \mathbf{X})^T) &= \mathbb{E}(\nu^\top \mathbf{X} \mathbf{X}^\top (\projnu)^\top) \\
                                                         &= \nu^\top \mathbb{E}(\mathbf{X}\mathbf{X}^\top)(\projnu)^\top \\
                                                         &= \nu^\top(\mathbb{V}(\mathbf{X})+\mathbb{E}(\mathbf{X})\mathbb{E}(\mathbf{X})^\top)(\projnu)^\top.
    \end{aligned}
  \end{equation}
  Let $X \sim \mathcal{MN}(\mu, \mathbf{U}, \mathbf{\Sigma})$. Then, let us state $\mathbb{V}(\mathbf{X}) = \mathbf{U} \operatorname{Trace}(\mathbf{\Sigma})$. We prove the previous equality as a technical lemma after the proof.

    In this case $\mathbb{V}(\mathbf{X}) = \mathbb{I}_n \operatorname{Trace}(\sigma^2\mathbb{I}_p) = \sigma^2 p \mathbb{I}_n$

  So
  \begin{equation}
    \begin{aligned}
      \mathbb{E}(\nu^\top\mathbf{X} (\projnu \mathbf{X})^T) &= \nu^\top(\sigma^2p\mathbb{I}_n + \mathbb{E}(\mathbf{X})\mathbb{E}(\mathbf{X})^\top)(\projnu)^\top \\
                                                            &= \sigma^2 p \nu^\top(\projnu)^\top + \nu^\top \mathbb{E}(\mathbf{X})\mathbb{E}(\mathbf{X})^\top(\projnu)^\top \\
                                                            &= \sigma^2 p \nu^\top(\mathbb{I}_n - \frac{\nu \nu^\top}{\norm{\nu}_2^2}) + \mathbb{E}(\nu^\top \mathbf{X})\mathbb{E}(\projnu \mathbf{X})^\top \\
                                                            &= \sigma^2 p (\nu^\top - \frac{\norm{\nu}_2^2\nu^\top}{\norm{\nu}_2^2}) + \mathbb{E}(\nu^\top \mathbf{X})\mathbb{E}((\projnu \mathbf{X})^\top) \\
                                                            &= \mathbb{E}(\nu^\top \mathbf{X})\mathbb{E}((\projnu \mathbf{X})^\top).
    \end{aligned}
  \end{equation}

  \textit{ii)}
  \begin{equation}
    \nu^T \mathbf{X} \sim \mathcal{MN}_{1 \times p}(\nu^\top\mu, \nu \nu^\top, \sigma^2\mathbb{I}_p)  = \mathcal{N}_p(\nu^\top \mu,\norm{\nu}_2^2\sigma^2 \mathbb{I}_p).
\end{equation}
So $\nu^T \mathbf{X} \sim \mathcal{N}_p(0,\norm{\nu}_2^2\sigma^2\mathbb{I}_p)$ under $\mathcal{H}_0$.
Yet, the density of a centered Gaussian random vector depends only on its norm, due to symmetry with respect to the origin. So $\norm{\nu^\top \mathbf{X}}$ and $\text{dir}(\nu^\top\mathbf{X})$ are independent.


%
%
\end{proof}


\begin{lemma}\label{lemma_trace} \Javier{Lemma missing!} \Jasmin{on prouve un reslutat qui est énoncé dans la preuve du lemme précédent Lemma \ref{lemma_indep_add_events}}

\end{lemma}

\begin{proof}[Proof of Lemma~\ref{lemma_trace}]
 
  \begin{equation}
    \begin{aligned}
      \text{Let} ~ \mathbf{Y} = \mathbf{X} - \mu \\
      \mathbb{V}(\mathbf{X}) &= \mathbb{E}[(\mathbf{X}-\mu)(\mathbf{X}-\mu)^\top] \\
                             &= \mathbb{E}(\mathbf{Y}\mathbf{Y}^\top).
  \end{aligned}
  \end{equation}
  And
  \begin{equation}
    \begin{aligned}
      (\mathbf{Y}\mathbf{Y}^T)_{ij} &= \sum_{k=1}^p \mathbf{Y}_{ik} \mathbf{Y}^\top_{kj} \\
                                    &= \sum_{k=1}^p \mathbf{Y}_{ik} \mathbf{Y}_{jk}.
    \end{aligned}
  \end{equation}
  Therefore,
  \begin{equation}
      \mathbb{E}((\mathbf{Y}\mathbf{Y}^\top)_{ij}) = \sum_{k=1}^p \mathbb{E}(\mathbf{Y}_{ik} \mathbf{Y}_{jk}).
  \end{equation}
  As $\mathbf{Y}$ is centered, by definition of a normal matrix we have:
  \begin{equation}
    \begin{aligned}
      \mathbb{E}((\mathbf{Y}\mathbf{Y}^\top)_{ij})  &= \sum_{k=1}^p \mathbf{U}_{ij}\Sigma_{kk} \\
                                                    &= \mathbf{U}_{ij} \sum_{k=1}^p \Sigma_{kk} \\
                                                   &= \mathbf{U}_{ij} \operatorname{Trace}(\Sigma).
    \end{aligned}
  \end{equation}

\end{proof}


\begin{proof}[Proof of Equation~\ref{riskcontrol} in Theorem \ref{thm:1}]
  From the definition \eqref{pval_final}\added{,} we have that for all $\mathbf{x} \in \mathbb{R}^{n \times p}$ 
  \begin{equation}
    \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\bigl(p(\mathbf{x}) \leq \alpha ~|~ C_1,C_2 \in \mathcal{C}(\mathbf{X}), \projnu \mathbf{X} = \projnu \mathbf{x}, \operatorname{dir}(\nu^\top \mathbf{X}) = \operatorname{dir}(\nu^\top \mathbf{x})\bigr) = \alpha\added{.}
  \end{equation}
  Therefore\added{,} we have
\begin{equation}
\begin{aligned}
& \mathbb{P}_{\mathcal{H}_0^{\{C_1,C_2\}}}\big(p(\mathbf{x}) \le \alpha 
    \,\big|\, C_1,C_2 \in \mathcal{C}(\mathbf{X})\big) \\
&= \mathbb{E}_{\mathcal{H}_0^{\{C_1,C_2\}}}
    \Big[
        \mathds{1}\{p(\mathbf{x}) \le \alpha\} 
        \,\big|\, C_1,C_2 \in \mathcal{C}(\mathbf{X})
    \Big] \\
&= \mathbb{E}_{\mathcal{H}_0^{\{C_1,C_2\}}}
    \Big[
        \mathbb{E}_{\mathcal{H}_0^{\{C_1,C_2\}}}
        \Big[
            \mathds{1}\{p(\mathbf{x}) \le \alpha\} \,\Big|\,
            C_1,C_2 \in \mathcal{C}(\mathbf{X}),
            \projnu \mathbf{X} = \projnu \mathbf{x}, \\
&\qquad\qquad\qquad
            \operatorname{dir}(\nu^\top \mathbf{X}) = \operatorname{dir}(\nu^\top \mathbf{x})
        \Big] 
        \,\Big|\, C_1,C_2 \in \mathcal{C}(\mathbf{X})
    \Big] \\
&= \mathbb{E}_{\mathcal{H}_0^{\{C_1,C_2\}}}
    \big[\alpha \,\big|\, C_1,C_2 \in \mathcal{C}(\mathbf{X})\big] \\
&= \alpha,
\end{aligned}
\end{equation}
  where the second equality follows from the law of total expectation.
\end{proof}


\section{Data Description}\label{app:data}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.2}
\caption{Description of the different populations of individuals. \\
\small EAS = East Asian, SAS = South Asian, AFR = African, EUR = European, AMR = admixed American.}
\label{table:indiv}

\begin{tabular}{>{\raggedright\arraybackslash}p{8cm} l l}
\toprule
\multicolumn{1}{c}{\textbf{Description}} &
\multicolumn{1}{c}{\textbf{Population}} &
\multicolumn{1}{c}{\textbf{Superpopulation}} \\
\midrule

Chinese Dai in Xishuangbanna, China & CDX & EAS \\
Han Chinese in Beijing, China & CHB & EAS \\
Japanese in Tokyo, Japan & JPT & EAS \\
Kinh in Ho Chi Minh City, Vietnam & KHV & EAS \\
Southern Han Chinese, China & CHS & EAS \\

Bengali in Bangladesh & BEB & SAS \\
Gujarati Indian in Houston, Texas & GIH & SAS \\
Indian Telugu in the UK & ITU & SAS \\
Punjabi in Lahore, Pakistan & PJL & SAS \\
Sri Lankan Tamil in the UK & STU & SAS \\

African Ancestry in Southwest US & ASW & AFR \\
African Caribbean in Barbados & ACB & AFR \\
Esan in Nigeria & ESN & AFR \\
Gambian in Western Division, The Gambia & GWD & AFR \\
Luhya in Webuye, Kenya & LWK & AFR \\
Mende in Sierra Leone & MSL & AFR \\
Yoruba in Ibadan, Nigeria & YRI & AFR \\

British in England and Scotland & GBR & EUR \\
Finnish in Finland & FIN & EUR \\
Iberian populations in Spain & IBS & EUR \\
Toscani in Italy & TSI & EUR \\
Utah residents with Northern and Western European ancestry & CEU & EUR \\

Colombian in Medellín, Colombia & CLM & AMR \\
Mexican Ancestry in Los Angeles, California & MXL & AMR \\
Peruvian in Lima, Peru & PEL & AMR \\
Puerto Rican in Puerto Rico & PUR & AMR \\

\bottomrule
\end{tabular}
\end{table}


\newpage



\bibliographystyle{abbrv}
\bibliography{biblio.bib}

\end{document}
